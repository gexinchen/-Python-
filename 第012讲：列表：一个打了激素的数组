#列表推导式
#[有关A的表达式 for A in B]
[i*i for i in range(10)]

#还原以下列表推导式
list1 = [(x, y) for x in range(10) for y in range(10) if x%2==0 if y%2!=0]

list2 = []
for x in range(10):
	if x % 2 == 0:
		for y in range(10):
			if y % 2 != 0:
				list1.append((x, y))
