#要求输入姓名，并且打印"你好,'姓名'"
name = input('请输入您的姓名：')
print ('你好，'+ name + '!')
#要求用户输入1到100之间数字并判断，输入符合要求打印“A”，不符合要求则打印“B”
temp = input('请输入1-100之间的数字')
number = int (temp)			#获取输入的整数
if (1 <= number <= 100):
	print ('A')
else:
	print ('B')
